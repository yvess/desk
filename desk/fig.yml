cdb:
  image: yvess/desk-couchdb
  hostname: cdb
  environment:
    - TERM=xterm-256color
  volumes:
    - ../:/opt/app/
    - /opt/src:/opt/src
    - /opt/env:/opt/env
  volumes_from:
    - data_desk
  ports:
    - "5984:5984"

dnsa:
  image: yvess/desk-dns
  hostname: dnsa.test
  environment:
    - TERM=xterm-256color
  volumes:
    - ../:/opt/app/
  volumes_from:
    - data_desk
  links:
    - cdb
  ports:
    - "1053:53"

dnsb:
  image: yvess/desk-dns
  hostname: dnsb.test
  environment:
    - TERM=xterm-256color
  volumes:
    - ../:/opt/app/
  volumes_from:
    - data_desk
  links:
    - cdb
  ports:
    - "2053:53"

foreman:
  image: yvess/desk-worker
  hostname: foreman
  environment:
    - TERM=xterm-256color
  volumes:
    - ../:/opt/app/
  volumes_from:
    - data_desk
  links:
    - cdb
    - dnsa
    - dnsb
